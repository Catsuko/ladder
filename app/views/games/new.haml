%h1 Create Game
- if @game.errors.any?
  %p.red.b Failed to create game, does it already exist?

= form_with(model: @game) do |form|
  = form.label(:name)
  = form.text_field(:name, required: true)
  .flex.flex-wrap.items-center
    = link_to('Back', games_path)
    = form.submit('Create')
